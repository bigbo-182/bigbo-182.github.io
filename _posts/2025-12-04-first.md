---
title: "Java 기초 개념 정리 – 객체지향 핵심 한 번에 이해하기"
date: 2025-01-01
categories: [Java, Study]
tags: [Java, OOP, 자바, 객체지향]
toc: true
author_profile: false
sidebar:
    nav: "docs"
---

# Java 기초 개념 정리 – 객체지향 핵심 한 번에 이해하기

처음 자바를 공부할 때 가장 많이 헷갈리는 부분이 바로 **객체지향(OOP)** 개념입니다.  
이번 글에서는 객체지향의 핵심 개념과, 자바에서 어떻게 구현되는지를 간단하면서도 명확하게 정리해보았습니다.

---

## 🔍 1. Java는 왜 객체지향 언어일까?

Java는 “모든 것을 객체로 바라본다”는 철학을 기반으로 만들어진 언어입니다.  
즉, 프로그램을 기능 중심이 아니라 **데이터(객체) 중심**으로 설계합니다.

### 객체지향의 핵심 4가지 특징

### ① 추상화 (Abstraction)
핵심만 남기고 불필요한 세부 사항을 숨기는 것  
예: 자동차 클래스를 만들 때 엔진 내부 구조까지 구현할 필요 없음

### ② 캡슐화 (Encapsulation)
데이터와 메서드를 하나로 묶고, 외부에서 직접 접근하지 못하게 보호  
→ `public`, `private` 같은 접근 제어자 사용

### ③ 상속 (Inheritance)
기존 클래스를 재사용하여 새로운 클래스를 만드는 것  
→ 코드 재사용성 증가

### ④ 다형성 (Polymorphism)
하나의 객체가 여러 형태로 동작할 수 있는 것  
→ 대표적으로 **오버라이딩**, **오버로딩**

---

## ⚙️ 2. 예제로 이해하는 객체지향

```java
class Animal {
    void sound() {
        System.out.println("동물이 소리를 냅니다.");
    }
}

class Dog extends Animal {
    @Override
    void sound() {
        System.out.println("멍멍!");
    }
}

class Cat extends Animal {
    @Override
    void sound() {
        System.out.println("야옹~");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal dog = new Dog();
        Animal cat = new Cat();

        dog.sound();  // 멍멍!
        cat.sound();  // 야옹~
    }
}
