---
title: "Java 배열 완전 정리 – 메모리, 복사, 다차원까지 한 번에 이해하기"
date: 2025-01-05
categories: [Java, Study]
tags: [Java, 배열, Array, 자료구조, 메모리]
toc: true
author_profile: false
sidebar:
    nav: "docs"
---

# Java 배열 완전 정리 – 메모리, 복사, 다차원까지 한 번에 이해하기

배열(Array)은 “같은 타입의 데이터를 모아두는 자료구조”라고 많이 배우지만,  
실제로는 **메모리 구조, 참조 타입, 복사 방식, 다차원 구조**까지 이해해야 제대로 쓸 수 있습니다.

이 글에서는 자바 배열을 다음 관점에서 정리합니다.

- 배열의 선언/생성/초기화  
- 배열이 “객체”라는 의미 (힙/스택 관점)  
- 얕은 복사 vs 깊은 복사  
- 다차원 배열(= 배열의 배열)  
- primitive 배열 vs 객체 배열 차이  
- Arrays 유틸리티  
- 실제로 자주 터지는 실수들  

---

# 🔍 1. 배열이 필요한 이유

학생 3명의 점수를 저장한다고 해보겠습니다.

```java
int score1 = 90;
int score2 = 85;
int score3 = 77;
```

학생 수가 30명, 100명으로 늘어나면 이런 방식은 불가능합니다.

그래서 배열을 사용합니다:

```java

int[] scores = new int[100];  // 학생 100명의 점수
```
배열을 사용하면

- 같은 타입 데이터를

- 연속된 인덱스로

- O(1) 속도로 접근할 수 있습니다.



# 🔍 2. 배열의 선언, 생성, 초기화
## ✔ 2-1. 배열 선언

```java
int[] numbers;      // 권장 방식
int numbers2[];     // 가능하지만 잘 쓰지 않음

```
## ✔ 2-2. 배열 생성 (new 사용)

```java
numbers = new int[3];
```

이 시점에 힙에는 [0, 0, 0] 이 저장됩니다.
(int의 기본값은 0)

## ✔ 2-3. 선언 + 생성 + 초기화

```java
int[] numbers = { 10, 20, 30 };
String[] names = { "철수", "영희", "민수" };
```

이 방식은 선언과 동시에 초기화할 때만 가능합니다.


# 🔍 3. 배열은 객체다 – 스택 & 힙 메모리 구조

아래 코드를 보겠습니다.

```java
int[] numbers = new int[3];
```

실제로 일어나는 일:

- 힙(Heap): 길이가 3인 int 배열 객체가 생성

- 스택(Stack): numbers 변수에 “배열 객체의 주소값” 저장

즉:

- 배열은 힙에 있는 객체

- 배열 변수는 그 객체를 가리키는 참조(reference)

예: 참조 공유 문제

```java
int[] a = new int[3];
int[] b = a;

a[0] = 100;

System.out.println(b[0]); // 100
```

둘은 같은 배열 객체를 가리키므로 값이 함께 바뀝니다.
여기서 얕은 복사(shallow copy) 문제가 나옵니다.


# 🔍 4. 배열의 기본값 (Default Value)

자바의 배열은 힙에서 자동으로 기본값으로 초기화됩니다.

| 타입                     | 기본값      |
| ---------------------- | -------- |
| byte, short, int, long | 0        |
| float, double          | 0.0      |
| char                   | '\u0000' |
| boolean                | false    |
| 참조 타입                  | null     |

```java
int[] nums = new int[3];       // [0, 0, 0]
String[] names = new String[3]; // [null, null, null]
```

---
# 🔍 5. 배열 순회

```java
int[] scores = { 90, 85, 77 };

// 1) 기본 for
for (int i = 0; i < scores.length; i++) {
    System.out.println(scores[i]);
}

// 2) 향상된 for
for (int score : scores) {
    System.out.println(score);
}
```

---
# 🔍 6. 얕은 복사 vs 깊은 복사

## ✔ clone 메소드

java 에는 객체를 복사하기 위한 메서드가 Object class 에 clone 메서드가있다.
clone 메서드의 설명을 보면 다음과 같다

```java
public class Object {

    /* ... 

    Creates and returns a copy of this object.
    if the class of this object does
    not implement the interface {@code Cloneable}, then a
    {@code CloneNotSupportedException} is thrown.
    
    */
    @IntrinsicCandidate
    protected native Object clone() throws CloneNotSupportedException;

}
```
- cloneable 인터페이스를 구현하지않으면 예외처리
- native 메서드 -> jvm 내부에서 객체복사 처리
- 접근제어자 protected -> 같은패키지 or 자식 클래스에서만 호출가능

## ✔ cloneable 인터페이스
```java
//Note that this interface does not contain the clone method. Therefore, it is not possible to clone an object merely by virtue of the fact that it implements this interface

public interface Cloneable {
}
```

- 설명에 보면 cloneable 인터페이스는 결국 복제가능한 클래스임을 나타내는 마커인터페이스


## ✔ clone 메서드 오버라이딩
```java
class Person implements Cloneable {
    private String name;
    public Person() {}

    @Override
    public Person clone() {
        try {
            Person clone = (Person) super.clone();
            // TODO: 이 복제본이 원본의 내부를 변경할 수 없도록 여기에 가변 상태를 복사합니다
            return clone;
        } catch (CloneNotSupportedException e) {
            throw new AssertionError();
        }
    }
}
```

- 자동완성된 예시를 보면 public 으로 오버라이딩하여 외부에서 접근가능하도록변경
- 기본적으로 Object(부모)의 clone 메서드 호출
- 주석에 원본의 내부를 변경할 수있다는 듯(?) 의 내용이 적혀있다

## ✔ 얕은 복사 (Shallow Copy)

```java 
//TIP 코드를 <b>실행</b>하려면 <shortcut actionId="Run"/>을(를) 누르거나
// 에디터 여백에 있는 <icon src="AllIcons.Actions.Execute"/> 아이콘을 클릭하세요.
public class Main {
    public static void main(String[] args) {

        Address address = new Address("서울");
        Person p1 = new Person("심대보", address);

        Person p2 = p1.clone(); // 얕은 복사

        // 복제본의 내부 객체(Address)의 값을 변경
        p2.address.city = "부산";
        p2.name = "김철수";

        // 원본도 바뀌었는지 출력 확인
        System.out.println("원본 이름: " + p1.name); // 심대보
        System.out.println("원본 주소: " + p1.address.city); // ❗ 부산
        System.out.println("복제 주소: " + p2.address.city); // 부산
    }
}

class Person implements Cloneable {
    String name;
    Address address;

    public Person(String name, Address address) {
        this.name = name;
        this.address = address;
    }

    @Override
    public Person clone() {
        try {
            return (Person) super.clone(); // shallow copy
        } catch (CloneNotSupportedException e) {
            throw new AssertionError();
        }
    }
}

class Address {
    String city;

    public Address(String city) {
        this.city = city;
    }
}
```

- 복사한 객체의 필드값을 변경하였는데 원본 데이터가 같이 변경됨 
- 참조타입의 필드가 있다면 원본데이터에 변경이 이루어짐 



## ✔ 깊은복사 (deep Copy)

```java
public class Main {
    public static void main(String[] args) {

        Address address = new Address("서울");
        Person p1 = new Person("심대보", address);

        Person p2 = p1.clone(); // 깊은 복사됨

        // 복제본 내부 객체 수정
        p2.address.city = "부산";

        // 출력 확인
        System.out.println("원본 주소: " + p1.address.city); // 서울 유지!
        System.out.println("복제 주소: " + p2.address.city); // 부산
    }
}

class Person implements Cloneable {
    String name;
    Address address;

    public Person(String name, Address address) {
        this.name = name;
        this.address = address;
    }

    @Override
    public Person clone() {
        try {
            Person clone = (Person) super.clone(); // shallow copy 기반

            // ❗ 깊은 복사: Address도 새로 만들어 복사해야 한다
            clone.address = new Address(this.address.city);

            return clone;

        } catch (CloneNotSupportedException e) {
            throw new AssertionError();
        }
    }
}

class Address {
    String city;

    public Address(String city) {
        this.city = city;
    }
}
```

# 🔍 7. 다차원 배열 – 사실은 “배열의 배열”

자바의 2차원 배열은 메모리 상의 완전한 2D 테이블이 아니라,
각 행이 또 하나의 배열을 가리키는 구조(배열의 배열) 입니다.

```java
int[][] map = new int[2][3];
```


내부적으로는:
map → 길이가 2인 배열

map[0], map[1] → 각각 길이가 3인 int 배열

## 7-1. 가변 길이 배열 (Jagged Array)

```java
int[][] jagged = new int[2][];  // 행 개수만 먼저 지정

jagged[0] = new int[3];   // 0번째 행: 길이 3
jagged[1] = new int[10];  // 1번째 행: 길이 10
```

- 각 행의 길이를 다르게 줄 수 있습니다.

---

# 🔍 8. Primitive 배열 vs 객체 배열
## ✔ 8-1. Primitive 배열 (예: int[])

```java
int[] nums = { 1, 2, 3, 4 };
```

- 값들이 메모리 상에 연속된 공간에 저장됩니다.

- CPU 캐시 효율이 좋아서 반복 연산 성능이 매우 좋습니다.


## ✔ 8-2. 객체 배열 (예: String[])

```java
String[] names = { "철수", "영희", "민수" };
```

- 배열 안에는 "철수" 객체가 직접 있는 게 아니라,
- 문자열 객체를 가리키는 참조값(reference) 이 들어 있습니다.
- 실제 문자열 객체들은 힙의 여기저기에 흩어져 있습니다.